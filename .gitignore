# Reálná data — NIKDY necommitovat
~/.bpdpmanager/
.bpdpmanager/
*.local.json
data/
private/
exports/
# Síťové zachytávky (HAR z prohlížeče) — mohou obsahovat reálná data ze STAG
*.har
# Diagnostické JS soubory stažené z MyQ/STAG (analýza loginu) — root-level
/js*.js
/prototype.js
# Diagnostické PDF (reálné posudky) v rootu — necommitovat
/*.pdf
# venv (i jako symlink) — bez lomítka, ať se chytí i symbolické odkazy
.venv*

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
*.egg-info/
*.egg
.eggs/
pip-wheel-metadata/
share/python-wheels/
MANIFEST

# Venv
.venv/
venv/
env/
ENV/

# Testy a nástroje
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/
.tox/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Logy
*.log

# Qt
*.pyc
*.qmlc
*.jsc
.qmake.cache
.qmake.stash
moc_*.cpp
moc_*.h
qrc_*.cpp
ui_*.h

# Lokální překlady
*.ts.bak
*.tmp

# Dočasná složka pro soubory předané Claudovi (reálná data — mimo Git)
Claude_TEMP/

# Session vestavěného prohlížeče STAG (cookies, cache) — patří do datové složky
# profilu (mimo repo); pojistka, kdyby si někdo dal data dovnitř repa
szz_webview/
